// postBuildInstructions.js
console.log(`
Post-Build Instructions:
1. Install 'serve' globally (if not already installed):
   npm install -g serve
   # or
   yarn global add serve

2. Run the server in the build directory:
   serve -s dist
`);